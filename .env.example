# Copy to .env for `netlify dev`, and set these in the Netlify UI for production.
# Use a key that is used by NOTHING ELSE, and set a hard spend cap on it in the
# Anthropic console BEFORE you deploy Phase 6. (R-3)
ANTHROPIC_API_KEY=sk-ant-...
ALLOWED_ORIGIN=https://your-site.netlify.app,http://localhost:8888
